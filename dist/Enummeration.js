"use strict";
//Enummerations - this actually stores a list of constants
; //if we assign a numerical value to first variable, compiler will automatically set the next value of others
let mySize = 2 /* Size.Medium */;
console.log(mySize);
